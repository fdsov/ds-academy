# M6.2 · Что ты освоишь

- [ ] Объяснишь разницу между `Series` и `DataFrame` и что такое индекс в pandas.
- [ ] Прочитаешь данные из CSV, Parquet и SQL и выберешь правильный формат для задачи.
- [ ] Уверенно отфильтруешь, отберёшь и преобразуешь данные через `loc`/`iloc`, булеву маску и `assign`.
- [ ] Разложишь любую агрегацию по схеме split-apply-combine и поймёшь разницу между `agg`, `transform` и `apply`.
- [ ] Корректно соединишь таблицы через `merge`/`join` и `concat`, не породив дубли.
- [ ] Перегонишь данные между широким и длинным форматом (`pivot`/`melt`) и поработаешь с временными рядами (`resample`, `rolling`).
- [ ] Напишешь тот же пайплайн на Polars в ленивом режиме и объяснишь, когда он быстрее pandas в 10-50 раз.
- [ ] Посчитаешь когорты, retention и агрегаты по игрокам на обоих движках.
