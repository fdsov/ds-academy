# M18.13 · Ты освоил это, когда

- Можешь на салфетке объяснить другу-нетехнарю, почему два линейных слоя без активации эквивалентны одному, и зачем нужен ReLU
- Выписываешь forward pass MLP в виде матричных умножений без подсматривания и понимаешь размерности каждой матрицы
- Объясняешь backpropagation через цепное правило: что такое $\delta^{(l)}$, почему производная активации входит в каждый шаг и как это связано с затуханием градиента
- Обоснованно выбираешь оптимизатор и learning rate, и понимаешь, чем AdamW отличается от Adam и почему это важно для weight decay
- Диагностируешь по поведению кривых лосса, что происходит: переобучение, слишком большой lr, неучащаяся сеть или утечка данных
- Пишешь рабочий обучающий цикл в PyTorch с нуля, не копируя, и знаешь, зачем нужны `zero_grad`, `model.eval()` и `BCEWithLogitsLoss`
