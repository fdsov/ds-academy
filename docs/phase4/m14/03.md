# M14.3 · Линейная регрессия: базовый кирпич

### Интуиция

Представь, что ты хочешь предсказать, сколько денег принесёт игрок за первый месяц (LTV), зная три числа: размер первого депозита, количество ставок в первый день и возраст аккаунта в часах до первой ставки. Линейная регрессия говорит простую вещь: возьмём каждый из этих факторов, умножим на свой «вес важности», сложим — и получим прогноз. Если первый депозит важен, его вес будет большим. Если возраст аккаунта почти ничего не значит, его вес будет около нуля.

Это самая честная модель из возможных: она прямо показывает, на сколько изменится прогноз, если один фактор вырастет на единицу. Никакой магии — взвешенная сумма.

Почему именно с неё начинают всё машинное обучение? Потому что в ней уже есть все ключевые понятия, которые потом встречаются везде: признаки и таргет, веса (параметры модели), функция потерь, оптимизация. Освоив линейную регрессию, ты понимаешь скелет любой модели — от логистической регрессии до нейросети. Они отличаются только тем, какую нелинейность накручивают поверх той же взвешенной суммы. Линейная регрессия — это «hello world» машинного обучения, но в отличие от учебного hello world её до сих пор массово используют в проде: она быстрая, интерпретируемая и не переобучается на ровном месте.

### Формально

Модель линейной регрессии:

$$
\hat{y} = w_0 + w_1 x_1 + w_2 x_2 + \dots + w_p x_p = \mathbf{w}^\top \mathbf{x}
$$

где $\mathbf{x}$ — вектор признаков (с добавленной единицей для свободного члена $w_0$), $\mathbf{w}$ — вектор весов, $\hat{y}$ — прогноз.

Чтобы найти веса, нужна мера «насколько мы ошибаемся». Стандартный выбор — средняя квадратичная ошибка (Mean Squared Error, MSE):

$$
L(\mathbf{w}) = \frac{1}{n} \sum_{i=1}^{n} \left( y_i - \mathbf{w}^\top \mathbf{x}_i \right)^2
$$

Почему квадрат, а не модуль? Во-первых, квадрат сильнее штрафует крупные промахи (промах в 10 хуже, чем два промаха по 5). Во-вторых, MSE гладко дифференцируема, что критично для оптимизации. В-третьих, минимизация MSE эквивалентна методу максимального правдоподобия при допущении, что шум распределён нормально — это связывает регрессию со статистикой.

### Два способа найти веса

Первый способ — аналитический. Производная MSE по весам приравнивается к нулю, и получается замкнутая формула, нормальное уравнение:

$$
\mathbf{w}^* = \left( \mathbf{X}^\top \mathbf{X} \right)^{-1} \mathbf{X}^\top \mathbf{y}
$$

где $\mathbf{X}$ — матрица признаков размера $n \times (p+1)$, $\mathbf{y}$ — вектор таргетов. Это точное решение за один шаг. Минус: обращение матрицы $\mathbf{X}^\top \mathbf{X}$ стоит порядка $O(p^3)$ и становится неподъёмным при большом числе признаков, а если признаки линейно зависимы (мультиколлинеарность) — матрица вырождается и обращение взрывается.

Второй способ — градиентный спуск. Мы стартуем со случайных весов и итеративно двигаемся в сторону уменьшения потерь:

$$
\mathbf{w} \leftarrow \mathbf{w} - \eta \, \nabla_{\mathbf{w}} L = \mathbf{w} + \frac{2\eta}{n} \sum_{i=1}^{n} \left( y_i - \mathbf{w}^\top \mathbf{x}_i \right) \mathbf{x}_i
$$

где $\eta$ — скорость обучения (learning rate). Градиент $\nabla_{\mathbf{w}} L$ указывает направление наискорейшего роста потерь, поэтому мы идём против него. Этот метод масштабируется на миллионы признаков и наблюдений и лежит в основе всего глубокого обучения.

!!! info "Интуиция градиентного спуска"

    Представь, что функция потерь — это рельеф местности в тумане, а ты хочешь спуститься в самую низкую точку долины. Ты не видишь дальше своих ног, но можешь почувствовать уклон под ногами. Логично делать шаг туда, где склон вниз круче всего. Размер шага — это learning rate: слишком большой шаг — перепрыгнешь долину и улетишь вверх по противоположному склону; слишком маленький — будешь спускаться вечность.

#### Разбор на пальцах: интерпретация веса в деньгах

Самая сильная сторона линейной регрессии — каждый вес читается напрямую. Допустим, мы обучили модель прогноза LTV игрока за первый месяц (в долларах) на одном признаке — размер первого депозита $x_1$ (тоже в долларах):

$$
\widehat{LTV} = 12 + 0.8 \cdot x_1
$$

Как это читать. Свободный член $w_0 = 12$ — это базовый прогноз LTV при нулевом депозите (формально экстраполяция, см. предупреждение ниже). Вес $w_1 = 0.8$ означает: каждый дополнительный доллар первого депозита добавляет к прогнозу LTV $0.80. Единицы у веса — это «доллар LTV на доллар депозита», то есть вес безразмерен по деньгам, но осмыслен как коэффициент.

Посчитаем для игрока с первым депозитом $50:

$$
\widehat{LTV} = 12 + 0.8 \cdot 50 = 12 + 40 = 52 \text{ доллара.}
$$

Теперь оценим эффект в деньгах от изменения. Маркетинг предлагает акцию, поднимающую средний первый депозит когорты с $50 до $60 (на $10). Прогнозируемый прирост LTV на одного игрока:

$$
\Delta \widehat{LTV} = 0.8 \cdot (60 - 50) = 0.8 \cdot 10 = 8 \text{ долларов.}
$$

Если когорта — 20 000 игроков в месяц, суммарный прогнозный прирост выручки:

$$
8 \times 20\,000 = 160\,000 \text{ долларов в месяц.}
$$

Проверка арифметики: $0.8 \times 10 = 8$, $8 \times 20\,000 = 160\,000$ — верно. Именно из таких расчётов рождается решение, стоит ли акция своих затрат: если она стоит дешевле $160 000 в месяц и не ломает другие метрики — она окупается. Но (важная оговорка) корреляция не равна причинности: вес 0.8 описывает связь в исторических данных, а не гарантирует, что искусственно поднятый депозит даст тот же LTV. Игроки, которые сами депозят больше, могут отличаться от тех, кого подтолкнула акция (это уже про каузальный вывод и A/B-тесты, не про саму регрессию).

### Допущения, которые все забывают

Линейная регрессия даёт корректные и интерпретируемые результаты только при выполнении ряда допущений: линейность связи (таргет действительно линейно зависит от признаков), независимость наблюдений, гомоскедастичность (разброс ошибки постоянен по всему диапазону прогнозов), нормальность остатков и отсутствие сильной мультиколлинеарности. В гемблинг-данных почти всегда нарушается гомоскедастичность — у крупных депозиторов разброс LTV огромен, у мелких маленький. Это не делает модель бесполезной, но означает, что доверительные интервалы коэффициентов нельзя читать буквально.

Ещё одно частое заблуждение — путать «модель линейна по признакам» с «модель рисует прямую линию». Линейность означает линейность по весам $\mathbf{w}$, а не по исходным переменным. Можно добавить признак $x^2$ или $\log(x)$ — модель останется линейной регрессией (взвешенная сумма) и при этом будет описывать кривую. Поэтому полиномиальная и логарифмическая зависимости прекрасно ложатся в тот же аппарат через feature engineering.

!!! warning "Типичная ошибка"

    Новичок видит большой вес у признака и объявляет его «самым важным». Это ловушка масштаба. Веса зависят от единиц измерения признака: депозит в долларах и депозит в центах дадут веса, отличающиеся в 100 раз, хотя признак тот же. Сравнивать важность признаков по сырым весам нельзя — сначала признаки надо привести к одному масштабу (стандартизация), либо смотреть на стандартизованные коэффициенты. Отдельная вершина той же ошибки — при мультиколлинеарности (например, «сумма депозитов» и «средний депозит» сильно коррелируют) веса становятся нестабильными и могут даже сменить знак при крошечном изменении данных, так что интерпретировать их по отдельности опасно.

!!! question "Проверь себя"

    1. Почему в MSE ошибки возводят в квадрат, а не берут по модулю? Назови минимум два довода.
    2. Чем аналитическое решение (нормальное уравнение) отличается от градиентного спуска и когда первое становится непригодным?
    3. За что отвечает learning rate $\eta$ и что произойдёт, если задать его слишком большим?
    4. Какое допущение линейной регрессии почти всегда нарушается на гемблинг-данных и к чему это приводит?
    5. Почему нельзя сравнивать важность признаков по сырым весам модели?

??? success "Ответы"

    1. Квадрат сильнее штрафует крупные промахи; MSE гладко дифференцируема (удобна для оптимизации); её минимизация эквивалентна максимуму правдоподобия при нормальном шуме.
    2. Нормальное уравнение даёт точное решение за один шаг через обращение $\mathbf{X}^\top\mathbf{X}$, но стоит $O(p^3)$ и взрывается при мультиколлинеарности; градиентный спуск итеративен и масштабируется на миллионы признаков и наблюдений.
    3. $\eta$ задаёт размер шага против градиента; слишком большой шаг перепрыгивает минимум, и спуск расходится (улетает вверх по противоположному склону).
    4. Гомоскедастичность: у крупных депозиторов разброс LTV огромен, у мелких маленький, поэтому доверительные интервалы коэффициентов нельзя читать буквально.
    5. Веса зависят от единиц измерения признаков (доллары против центов меняют вес в 100 раз), поэтому сначала нужна стандартизация или стандартизованные коэффициенты; вдобавок мультиколлинеарность делает отдельные веса нестабильными.

!!! example "Мини-задание"

    На бумаге или в Python посчитай один шаг градиентного спуска для одной точки. Дано: $x=2$, $y=5$, текущий вес $w=1$ (без свободного члена, $\hat{y}=wx$), $\eta=0.1$. Вычисли прогноз, остаток $y-\hat{y}$, градиент по формуле из текста и новое значение $w$. Убедись, что вес сдвинулся в сторону уменьшения ошибки. Подсказка для самопроверки: прогноз $\hat{y}=2$, остаток $3$, шаг увеличивает $w$ (мы недооценили $y$).

### Быстрый квиз

Выбери вариант — ответ подсветится сразу.

```quiz
Что физически означает вес $w_1$ в линейной регрессии $\hat{y}=w_0+w_1 x_1$?
[ ] вероятность того, что признак важен
[x] на сколько изменится прогноз при росте признака на единицу
[ ] долю объяснённой дисперсии таргета
> Линейная регрессия — взвешенная сумма; вес — это приращение прогноза на единицу признака.
---
Почему в MSE ошибки возводят в квадрат, а не берут по модулю?
[ ] чтобы результат всегда был целым числом
[x] квадрат сильнее штрафует крупные промахи и гладко дифференцируем
[ ] чтобы убрать свободный член из модели
> Квадрат усиливает штраф за большие ошибки и даёт гладкую производную, удобную для оптимизации; вдобавок это совпадает с максимумом правдоподобия при нормальном шуме.
---
Когда аналитическое решение (нормальное уравнение) становится непригодным?
[ ] когда таргет измерен в долларах
[x] при очень большом числе признаков или сильной мультиколлинеарности
[ ] всегда, его не используют на практике
> Обращение $\mathbf{X}^\top\mathbf{X}$ стоит $O(p^3)$ и взрывается, если признаки линейно зависимы; тогда переходят к градиентному спуску или регуляризации.
---
Что произойдёт при слишком большом learning rate $\eta$?
[ ] спуск гарантированно найдёт минимум быстрее
[x] шаги перепрыгивают минимум и оптимизация расходится
[ ] модель станет нелинейной
> Слишком большой шаг улетает вверх по противоположному склону долины потерь — потери растут вместо убывания.
---
Модель обучена: $\widehat{LTV}=12+0.8\cdot x_1$, где $x_1$ — первый депозит в долларах. На сколько вырастет прогноз LTV, если депозит вырастет с 50 до 60 долларов?
[ ] на 48 долларов
[x] на 8 долларов
[ ] на 60 долларов
> $\Delta\widehat{LTV}=0.8\cdot(60-50)=0.8\cdot 10=8$ долларов на игрока.
---
Почему нельзя сравнивать важность признаков по сырым весам?
[ ] веса всегда положительные
[x] веса зависят от единиц измерения признаков, нужна стандартизация
[ ] сырые веса не вычисляются на практике
> Один и тот же признак в долларах или центах даст веса, отличающиеся в 100 раз; для сравнения важности признаки приводят к общему масштабу.
```
