# M1.8 · Ноутбуки: Jupyter, JupyterLab, marimo

Ноутбук — интерактивный документ, где код, его вывод, графики и текст живут вместе. Для исследования данных это незаменимо: ты видишь результат каждого шага сразу.

Простыми словами: ноутбук — это блокнот, где ты пишешь кусочек кода в ячейке, жмёшь запуск и тут же видишь под ней результат — таблицу, график, число. Идеально, чтобы крутить данные руками и думать.

Чтобы интуиция была точнее: ноутбук состоит из двух частей. Первая — сам файл с ячейками (код и текст), который ты видишь. Вторая — ядро (kernel): отдельный живой Python-процесс, который держит в памяти все переменные и выполняет код. Файл и ядро — это разные сущности, и почти все боли ноутбуков растут именно из того, что между ними легко возникает рассинхрон. Запомни эту пару «файл vs ядро» — к ней мы будем возвращаться весь шаг.

### Jupyter Notebook против JupyterLab

Jupyter Notebook — классический интерфейс, одна вкладка с одним ноутбуком. JupyterLab — это среда вокруг той же технологии: вкладки, файловый менеджер, терминал, несколько ноутбуков рядом, расширения. На 2026 JupyterLab — стандартный выбор, если идёшь по экосистеме Jupyter. «Классический» Notebook 7 теперь сам построен на технологиях Lab.

Важная деталь под капотом: и Notebook, и JupyterLab сохраняют файл в формате `.ipynb` — это JSON. Внутри лежат не только ячейки с кодом, но и их сохранённый вывод (числа, текст, даже base64-картинки графиков) и метаданные вроде `execution_count` — номера, под которым ячейка была выполнена в последний раз. Из-за того что вывод хранится прямо в файле, два визуально одинаковых ноутбука могут давать огромный нечитаемый дифф в git: поменялась одна цифра в графике — изменились тысячи строк base64. Это первая практическая причина, почему ноутбуки тяжело ревьюить и версионировать.

### Проблема скрытого состояния

Это главная боль Jupyter, и на ней горят даже опытные люди. Понять её обязательно.

Простыми словами: в Jupyter ты можешь запускать ячейки в любом порядке и сколько угодно раз. Запустил ячейку 5, потом 2, потом снова 5, потом удалил ячейку 3, в которой определил переменную. На экране всё выглядит работающим — но это иллюзия. Переменная всё ещё живёт в памяти, хотя кода, который её создал, в ноутбуке уже нет. Дай этот ноутбук другому — у него ничего не запустится, потому что состояние было «спрятано» в памяти твоей сессии, а не в коде.

Формально: проблема hidden state — рассинхрон между видимым кодом ноутбука и фактическим состоянием ядра (kernel namespace). Ядро хранит все переменные от всех когда-либо выполненных ячеек, независимо от текущего содержимого и порядка ячеек на экране. Результат — ноутбук, который воспроизводится только в твоей конкретной живой сессии и нигде больше. Исследования воспроизводимости публичных Jupyter-ноутбуков показывают, что значительная их часть не исполняется сверху вниз без ошибок.

Разберём конкретный сценарий, чтобы увидеть баг вживую. Числа слева — `execution_count`, то есть порядок, в котором ячейки реально выполнялись, а не их позиция на экране:

```python
# Ячейка A (выполнена 1-й)
ggr = 100_000          # gross gaming revenue за день, $

# Ячейка B (выполнена 2-й)
bonus_cost = 0.20 * ggr   # стоимость бонусов, $

# Ячейка C (выполнена 3-й)
net = ggr - bonus_cost
print(net)   # 80000.0
```

Теперь ты возвращаешься к ячейке A, меняешь `ggr = 100_000` на `ggr = 50_000` и перезапускаешь только её (execution_count становится 4). Ячейку B ты НЕ перезапускал. В памяти ядра `bonus_cost` всё ещё равен `20_000` (посчитан от старого GGR), а `ggr` уже `50_000`. Если теперь снова выполнить ячейку C, получишь `net = 30_000` — число, которое не соответствует ни старому, ни новому состоянию кода. Оно «правильное» только для несуществующей комбинации. В аналитике гемблинга это именно тот класс ошибок, который тихо протекает в отчёт: цифра выглядит правдоподобной, дашборд зелёный, а на деле net GGR посчитан от рассогласованных промежуточных величин.

Ещё одна типичная ловушка — порядок исполнения, который читается по `execution_count` в скобках слева от ячейки `[1]`, `[2]`, `[7]`. Если номера идут не подряд (`[1]`, `[7]`, `[3]`), это прямой сигнал, что ноутбук гоняли вразнобой и сверху вниз он может не пройти. Пустые скобки `[ ]` означают, что ячейку ни разу не выполняли в текущей сессии — её переменных в памяти просто нет.

!!! tip "Дисциплина Jupyter"

    Если работаешь в Jupyter — регулярно делай Restart Kernel and Run All. Это перезапускает ядро с чистого листа и прогоняет все ячейки сверху вниз. Если так не запускается — твой ноутбук сломан, просто ты этого ещё не заметил. Делай это перед каждым коммитом.

Почему именно Restart, а не просто Run All: Run All прогоняет ячейки сверху вниз, но поверх уже загрязнённого namespace — старые переменные остаются в памяти и могут маскировать ошибку (например, удалённый импорт всё ещё «работает», потому что модуль импортирован ранее). Только Restart гарантированно очищает namespace до пустого, воспроизводя ситуацию свежего читателя, который открыл твой файл с нуля. Это и есть единственная честная проверка воспроизводимости.

### marimo: почему набирает в 2026

marimo — ноутбук нового поколения, который архитектурно убирает проблему скрытого состояния. К 2026 он быстро набирает популярность именно поэтому.

Простыми словами: marimo — это реактивный ноутбук. Он понимает, какая ячейка от какой зависит. Изменил переменную в одной ячейке — все ячейки, которые её используют, автоматически пересчитываются, а устаревшие помечаются. Удалил ячейку — переменная исчезает из памяти тоже. Состояние на экране всегда соответствует реальности. Плюс сам файл — это обычный `.py`, а не JSON, поэтому он нормально дружит с git (видно человеческие дифы).

Вернёмся к примеру с GGR. В marimo ячейка `bonus_cost = 0.20 * ggr` явно зависит от `ggr`. Как только ты меняешь `ggr` на `50_000`, marimo сам видит по графу зависимостей, что `bonus_cost` и `net` устарели, и перевыполняет их в правильном порядке. Получить рассогласованное `net = 30_000`, как в Jupyter, физически невозможно — рассинхрон между кодом и памятью по построению не возникает.

Формально: marimo строит DAG (направленный ациклический граф) зависимостей между ячейками через статический анализ кода. При изменении ячейки он детерминированно перевыполняет все зависимые ячейки в топологическом порядке, гарантируя консистентность состояния. Запрещены циклические зависимости и повторное определение одной переменной в разных ячейках — это исключает классы багов Jupyter. Файл хранится как исполняемый Python-модуль, что даёт чистые git-дифы, импортируемость и запуск как обычного скрипта или как веб-приложения.

```python
# Пример ячеек marimo. Зависимости выводятся автоматически.
import marimo as mo
import polars as pl

df = pl.read_parquet("data/processed/deposits.parquet")

# Эта ячейка зависит от df. Изменишь df — она пересчитается сама.
ftd = df.filter(pl.col("is_first_deposit")).select(pl.col("amount").mean())
```

Цена этой строгости — некоторые привычки Jupyter в marimo не работают, и это осознанный размен. Нельзя переопределить переменную в двух ячейках (придётся переименовать или объединить ячейки), нельзя строить циклические зависимости, а чтобы переиспользовать имя в цикле без утечки в граф — его прячут внутри функции или дают префикс `_` (тогда переменная локальна для ячейки и в DAG не попадает). Для интерактивных виджетов вместо «сырых» глобальных переменных используют `mo.ui.slider` и подобные элементы. То есть marimo меняет немного свободы на гарантию воспроизводимости — ровно тот размен, который выгоден для командного и продового кода.

Прагматичный вывод на 2026: для быстрого личного исследования Jupyter/JupyterLab всё ещё повсеместны и это нормально. Но для воспроизводимых аналитических отчётов, для кода, который пойдёт в команду или в прод, и для дашбордов marimo — лучший выбор, и стоит освоить его сейчас, пока это даёт преимущество.

!!! warning "Частые ошибки новичков"

    - Думать, что «ячейка выполнилась без ошибки» = «ноутбук воспроизводим». Это разные вещи: ошибка могла быть скрыта порядком запуска. Проверка — только Restart and Run All.
    - Коммитить `.ipynb` с тяжёлым сохранённым выводом (картинки в base64), засоряя git. Очищай вывод перед коммитом или используй инструменты вроде `nbstripout`.
    - В marimo пытаться переопределить одну переменную в двух ячейках и не понимать, почему ругается, — это не баг, а защита от hidden state.

!!! question "Проверь себя"

    1. В чём суть проблемы скрытого состояния (hidden state) в Jupyter?
    2. Что делает Restart Kernel and Run All и зачем гонять его перед каждым коммитом?
    3. За счёт чего marimo архитектурно устраняет hidden state и почему его `.py`-файл дружит с git?

??? success "Ответы"

    1. Рассинхрон между видимым кодом и состоянием ядра: ядро хранит все переменные от всех когда-либо выполненных ячеек независимо от текущего содержимого и порядка. Ноутбук «работает» только в живой сессии и нигде больше.
    2. Перезапускает ядро с нуля и прогоняет все ячейки сверху вниз. Если падает — ноутбук на самом деле сломан; проверка ловит это до коммита.
    3. marimo строит DAG зависимостей между ячейками статическим анализом и детерминированно перевыполняет зависимые ячейки в топологическом порядке; запрещены циклы и повторное определение переменной. Файл — исполняемый Python-модуль, отсюда чистые git-дифы.

!!! example "Мини-задание"

    Открой любой свой Jupyter-ноутбук и сделай Restart Kernel and Run All. Запиши, упал он или нет и на какой ячейке. Если упал — это и есть hidden state вживую; кратко опиши, какая переменная «жила» в памяти без кода.

### Быстрый квиз

Выбери вариант — ответ подсветится сразу.

```quiz
Что такое «ядро» (kernel) в контексте ноутбука?
[ ] Файл .ipynb, в котором сохранены ячейки и их вывод
[x] Живой Python-процесс, который держит в памяти все переменные и выполняет код
[ ] Расширение JupyterLab для управления вкладками
> Файл — это видимый код, а ядро — отдельный процесс с памятью переменных. Рассинхрон между ними и порождает hidden state.
---
В чём суть проблемы скрытого состояния (hidden state) в Jupyter?
[ ] Ноутбук сохраняет вывод графиков прямо в файл и раздувает git
[x] Состояние ядра рассинхронизировано с видимым кодом: в памяти живут переменные от всех выполненных ячеек независимо от их текущего содержания и порядка
[ ] Ядро автоматически удаляет переменные при закрытии вкладки
> Ядро помнит всё, что когда-либо выполнялось, поэтому ноутбук «работает» только в живой сессии, а у другого человека падает.
---
Почему перед коммитом советуют именно Restart Kernel and Run All, а не просто Run All?
[ ] Restart быстрее выполняется
[ ] Run All вообще не выполняет ячейки сверху вниз
[x] Только Restart очищает namespace до пустого, иначе старые переменные могут маскировать ошибку
> Run All поверх загрязнённой памяти может «пройти» из-за оставшихся переменных. Чистая проверка воспроизводимости — только с перезапуском ядра.
---
За счёт чего marimo архитектурно устраняет hidden state?
[ ] Сохраняет все переменные на диск после каждой ячейки
[x] Строит DAG зависимостей статическим анализом и перевыполняет зависимые ячейки в топологическом порядке
[ ] Запускает каждую ячейку в отдельном процессе
> marimo детерминированно пересчитывает зависимые ячейки и запрещает циклы и повторное определение переменной — рассинхрон по построению невозможен.
---
Почему .py-файл marimo дружит с git лучше, чем .ipynb Jupyter?
[ ] Он меньше по размеру всегда
[x] Это обычный исполняемый Python-модуль, а .ipynb — JSON с сохранённым выводом, дающий шумные дифы
[ ] Git не умеет открывать JSON-файлы
> .ipynb хранит вывод (включая base64-картинки) внутри JSON, поэтому дифы огромные и нечитаемые; .py marimo даёт человеческие дифы.
```
